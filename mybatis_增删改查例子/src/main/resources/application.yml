server:
  port: 80

# database config
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mybatis_demo?useUnicode=true&serverTimezone=UTC&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis config
mybatis:
  type-aliases-package: com.wcc.model
  mapper-locations: classpath:mybatis/mapper/*.xml
